#include <stdio.h>

int add(int num1,int num2)
{
    int sum;
  sum= num1*num2;
  return sum;
} int main()
{
    int var1,var2;
    printf("enter two numbers: ");
    scanf("%d%d",&var1,&var2);
    int res = add(var1,var2);
    printf("output: %d",res);
    return 0;
}
 
