#include <stdio.h>
int increment(int var)

{  var = var+1;
    return var;
} int main()
{
    int num1 = 12;
    int num2 = increment(num1);
    printf("the number is %d",num1);
    printf("increment number is %d",num2);
    return 0;
}
