#include <stdio.h>
    
int main(int argc, char const *argv[])
{
int i,avg,sum=0;
printf("Disply the first 10 natural no\n");
for ( i = 1; i <= 10; i++)
{
sum=sum+i;
printf("The number-%d : %d\n",i,i);
}
printf("The sum is %d\n",sum);
avg=sum/10;
printf("The averae is %d\n",avg);
    return 0;
}
