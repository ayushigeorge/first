#include <stdio.h>

int main()
{
    int n,num,sum=0;
    printf("Hello World: ");
    scanf("%d",&n);
    while(n!=0){
        n=n%10;
        num= num/10;
        sum = sum+ n;
    }
    printf("%d is reverse number",sum);

    return 0;
}
